# 2026-07-16 15:57 交易看板对话备份

## 用户要求

- 用户截图指向独立“隔夜美股映射”页面，并表示该项也不需要，要求删除。

## 本轮处理

- 删除侧栏“隔夜映射”导航及完整页面区块，后续导航重新编号为四项。
- 删除前端独立美股大盘与海外映射渲染代码及专用样式。
- 删除 `config.json` 中标普500、纳斯达克、费城半导体、SMH、SOXX宽基/行业卡片配置。
- 后端不再下载这些未被其他模块使用的美股行情，只请求持仓篮子实际需要的海外同行。
- 保留长电科技同行篮子中的 AMKR、ASX；二者继续显示纽约市场日与隔夜收益，并继续排除在A股同行排名、中位数和强弱计算之外。
- 看板版本更新为 V1.6，README与自动化断言同步更新。

## 验证结果

- 本地浏览器：四个导航、零个隔夜入口、零个隔夜页面、两个海外同行行；资金去向和每日日记均正常。
- 控制台错误：0。
- 390px视口：无横向溢出。
- `python3 -m py_compile dashboard/server.py`、`node --check dashboard/app.js`、JSON校验与 `git diff --check` 均通过。
- Playwright：4项测试全部通过。
