# 2026-07-16 16:47 交易看板对话备份

## 用户要求

- 打通 `交易记录.xlsx → 当前持仓 → 同行库 → 看板`。
- 同行完全由AI自动认定，不再要求人工确认。

## 实现结果

- 新增 `dashboard/portfolio_sync.py`，负责Excel持仓同步、代码/方向归一、成本重算、文件变化检测和AI同行生成。
- 新增 `position_seed.json`：由于历史Excel存在已知漏单、代码错误和错误剩余仓位，以用户已确认的长电900股、科创50ETF100份作为第198行基准；只重放之后新增成交，避免已清仓标的被错误复活。
- `config.json` 不再保存静态持仓和A股同行，仅保存Excel路径、AI参数和既有海外映射。
- 新增持仓后，系统自动识别股票/ETF、生成4个同行、写入 `peer_library.json`，无需修改配置或重启服务。
- 股票使用 Tushare 行业、主营业务和经营范围进行本地稀有语义重合排序；ETF按业绩基准、名称和投资类型识别。
- API新增 `/api/portfolio`；主接口返回Excel更新时间、总行数、新增重放行数和 `portfolio_version`。
- 前端检测到 `portfolio_version` 变化后自动清除1/5/20日旧缓存，重新生成同行擂台。
- 同行卡片展示AI模型、置信度、整体依据和每只同行的共同业务语义。

## 当前真实输出

- Excel数据行：197；基准后新增成交：0。
- 当前持仓：长电科技900股、科创50ETF100份。
- 长电AI同行：蓝箭电子、华天科技、晶方科技、气派科技；置信度84%。
- 科创50ETF：4只相同业绩基准产品；置信度96%。
- AMKR、ASX继续保留在长电篮子底部，仍不进入A股同行排名。

## 验证

- Python单元测试5项通过：代码归一、持仓切换、文件变化热更新、真实基准重建、AI语义同行排序。
- Playwright 4项通过：真实数据、AI证据、海外同行、5日切换、刷新、日记、交易时段定时器和390px移动端。
- 正式8765页面：2个持仓组、8条AI同行证据、2条海外同行、控制台错误0、无横向溢出。
