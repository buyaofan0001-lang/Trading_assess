# Tushare 自建数据接口接入

- 时间：2026-07-16 12:43
- 用户请求：保存统一 Tushare 初始化方式，后续引用同一文件，并先完成数据接口接入。

## 完成结果

- 复用仓库已有统一入口 `tushare_client.py`，未创建重复初始化文件。
- 统一调用方式为 `from tushare_client import get_pro` 后执行 `pro = get_pro()`。
- `get_pro()` 会把 Tushare Pro 对象指向用户指定的自建 HTTP 接口。
- `ts.pro_bar` 必须显式使用 `api=pro`。
- Token 从环境变量 `TUSHARE_TOKEN` 或本地 `tushare_token.txt` 读取，不写入受 Git 管理的源代码。
- 已确认 `tushare_token.txt` 被 `.gitignore` 排除、未被 Git 跟踪，并将文件权限调整为 `600`。

## 验证

执行 `python3 tushare_client.py`：

- `pro.index_basic(limit=5)` 成功返回 5 条指数基础数据。
- `ts.pro_bar(api=pro, ts_code="000001.SZ", limit=3)` 成功返回最近 3 条行情。

接口现已可用；后续项目内所有结构化行情取数统一引用该入口。
