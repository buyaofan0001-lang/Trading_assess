# 2026-07-18 14:30｜风控模型自动同步与重跑

## 用户追问与需求升级

- 用户发现科技板块风控雷达仍显示2026-07-16，询问为什么没有使用2026-07-17数据。
- 用户明确要求：面板不仅读取结果，还必须自动同步输入数据并重跑模型。

## 根因

- 自建Tushare当前已经可以返回2026-07-17沪深300日线，因此不是接口缺数。
- 模型增量更新器曾在2026-07-17数据尚未发布时查询到空表，但 coverage sidecar 仍写入 `queried_end=20260717`。
- 后续刷新只看sidecar，误以为该日已经覆盖；实际CSV最大日期仍是2026-07-16，因此7月17日被永久跳过。

## 修复

- 修复模型包 `effective_coverage_frame`：sidecar只用于保护序列上市前的历史头部，实时尾部严格以CSV真实行日期判断。
- 新增回归测试，证明“sidecar标到7月17日、CSV只有7月16日”时仍会请求7月17日。
- 重新执行 `refresh_data.py --end-date 20260717`，补齐国内指数、美股/ETF、VIX系列等7月17日数据；USDCNH更新到7月16日，处于允许的一交易日滞后。
- 重新执行冻结V8：`run_model.py --end-date 20260717 --output-dir results`。模型特征、权重、阈值和冻结指纹均未改变。

## 最新正式结果

- 信号日：2026-07-17。
- 数据时效：`fresh`。
- 原始风险分数：0.2479%。
- 校准路径风险率：0.3727%。
- 状态：`diagnostic_only_already_weak`，没有新预警。
- 安全边界：低分仍不能解释为安全或买入许可；自动仓位动作不可用。

## 看板V1.11自动链路

- 后台每30分钟查询上交所最近已收盘交易日；工作日17:45前使用上一交易日，17:45后使用当日，周末/节假日按交易日历回退。
- 正式信号落后时，独立后台线程先刷新输入数据，再重跑冻结V8并更新正式结果，不阻塞看板行情读取。
- 页面每60秒读取结果和运行状态；重跑成功后自动显示新信号。
- 新增“立即同步并重跑”按钮及 `POST /api/risk-model/refresh`。
- 同步状态与日志分别写入 `dashboard/runtime/risk_model_sync_state.json` 和 `dashboard/runtime/risk_model_sync.log`；失败保留旧结果并明确报错。

## 验收

- 实时API返回信号日2026-07-17、数据时效fresh、同步状态current、目标交易日2026-07-17。
- 模型增量更新测试5项通过。
- Playwright浏览器测试8项通过，包含手动非阻塞重跑、自动轮询和390px移动端无溢出。
- 真实浏览器确认V1.11资源、7月17日信号、fresh标签、自动同步状态和手动重跑按钮均可见。
- 组合单元测试存在一项与本轮无关的旧预期：当前Excel链路重建长电科技1100股，而测试仍写死900股；本轮未擅自修改持仓口径。
