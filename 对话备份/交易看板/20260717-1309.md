# 盘前自动任务与看板修复｜2026-07-17 13:09

## 用户指令

在确认7月17日盘前报告未生成后，用户要求开始修复。

## 根因与修复

- 原盘前自动任务 `a-2` 使用 `gpt-5.6-sol/xhigh`，单轮要求穷举跨资产行情、多行业ETF、新闻、公告、社交媒体及双来源核验；2026-07-16、07-17连续没有写出正式文件，7月17线程状态为 `systemError`。
- 保留工作日08:30计划，将模型改为 `gpt-5.6-terra/high`，并把流程改成有界容错：2分钟内先写当日骨架，8分钟停止新增搜索，12分钟内完成；每个核心催化最多2个来源、全文链接不超过12个；数据源失败必须标缺口，不能阻断落盘。
- 已在失败线程中手动补跑7月17日。13:06先生成最小骨架，13:09扩展为完整报告，正式路径为 `/Users/liuguan1/Documents/New project 2/logs/pre-market/2026/2026-07-17.md`。
- 补跑报告明确区分原盘前窗口与延迟补跑后的信息，不把当天A股盘中走势倒灌成盘前依据；正文包含数据截止时间、缺口、非投资建议、隔夜温度、行业映射、长电900股持仓风险和行动清单。

## 看板防误读升级

- `/api/reports` 新增两类报告的健康状态。
- 工作日09:00后缺盘前报告、17:00后缺收盘报告时，栏目显示红色“今日未生成”提示，同时保留最后一篇历史报告；节假日文案只提示确认交易日，不武断宣称任务失败。
- 当日报告生成后，60秒轮询自动切换到最新文件并消除告警。

## 验证

- 后端共9项Python测试通过，包括到期缺失判断。
- 浏览器共6项Playwright测试通过，包括缺失提示、自动轮询和390px手机无横向溢出。
- 实际接口已识别盘前最新日期 `2026-07-17`，健康状态为 `current`；手机端显示最新日期、无告警、无横向溢出。
