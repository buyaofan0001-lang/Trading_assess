# 交易看板对话备份｜2026-07-18 11:57

## 用户授权

用户在确认本地服务停止原因后，明确要求“配置”macOS长期后台运行。

## 已实施

- 安装用户级LaunchAgent：`~/Library/LaunchAgents/com.liuguan.trading-assess.dashboard.plist`。
- 固定使用 `/opt/anaconda3/bin/python3` 运行项目的 `dashboard/server.py`。
- 设置登录自启、异常退出自动重启、10秒重启节流。
- 服务只绑定 `127.0.0.1:8765`，没有向局域网或公网开放。
- 标准输出与错误日志写入 `dashboard/runtime/`，该目录已加入Git忽略。

## 验证

- plist语法检查通过。
- `launchctl print` 显示 `state = running`。
- 端口8765由Python进程监听。
- `/api/portfolio` 返回科创50ETF 100份、长电科技900股，错误列表为空。
- 加载及kickstart过程中系统终止旧进程并成功重新拉起，验证KeepAlive有效。

## 使用边界

本轮只解决本机服务持久化。电脑关机、注销或深度睡眠时无法访问；手机/LAN/公网访问尚未开放，后续必须单独设计监听地址、鉴权和网络安全。
