# 2026-07-18 13:17｜科技板块风控模型接入看板

## 用户需求

用户提供风控模型交付目录：

`/Users/liuguan1/Documents/github/backtest/backtest_project/deliverables/tech_sector_risk_model_v8_20260718`

希望把该模型加入现有“守门台”交易看板。

## 本轮处理

- 审计模型包的 README、MODEL_CARD、验证报告、冻结清单、运行入口和正式输出，确认模型边界为科技板块未来1–2日风险起点研究预警，部署状态为 `research_only`。
- 看板新增“科技板块风控雷达”栏目和 `/api/risk-model`，只读消费 `results/current_warning_brief.json`、`results/forward_shadow_summary.csv` 与冻结清单元数据，不复制参数、不重新实现模型、不改写冻结包。
- 前端展示原始风险分数、冻结阈值、校准路径风险率、数据时效、运行状态、推高/压低风险因子、前瞻影子样本进度和冻结指纹；页面可见时每60秒检查正式结果文件。
- 固定安全说明：数据过期只作诊断；模型低分不等于安全或买入；自动仓位控制器未通过，不能生成自动减仓、卖出或买入动作。
- 当前正式结果：信号日2026-07-16，原始分数2.3367%，校准路径风险率1.5101%，状态 `diagnostic_only_already_weak`；`USDCNH.FXCM` 数据过期；前瞻影子成熟样本0/252、正事件0/25。
- 冻结模型指纹保持 `c849959c0a5fa54114eecb1e5f91b6bc835c00d25d1e40b922b541608e8344ae`，未修改模型参数、权重或阈值。

## 验证

- Python单元测试：13项通过。
- Playwright浏览器测试：7项通过。
- 本机运行接口返回正式模型状态；真实浏览器确认V1.10资源、10条因子、风险降级文案和桌面端零横向溢出。
- macOS LaunchAgent继续管理本地服务，入口为 `http://127.0.0.1:8765/?v=1.10.0#risk-model`。

## 长期记忆蒸馏

已同步更新 `memory.md`：风控模型只能作为只读研究预警源，不能被看板包装成自动交易控制器；数据过期必须显式降级，低分也不是交易许可。
