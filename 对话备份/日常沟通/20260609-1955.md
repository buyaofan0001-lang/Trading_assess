# 2026-06-09 19:55 日常沟通 - 记忆系统问题

## 用户反馈
用户指出：虽然双方之间已经有很多归档聊天记录，但每次助手似乎只读 `AGENTS.md`，没有把全部历史聊天整合成长期记忆；希望助手像“老熟人”一样，能在以后每次对话中了解用户习惯，而不是只把聊天记录存档。

## 助手判断
- 归档聊天记录本身不是稳定记忆。
- 稳定记忆应由 `memory.md` 承担，`对话备份/` 与 `复盘/` 应作为可检索的原始证据库。
- 当前缺口是：`memory.md` 的主体整合停在 2026-04-26，且 `AGENTS.md` 没有明确要求按话题检索历史归档。

## 已执行更新
- 更新 `AGENTS.md`：新增“记忆加载与归档使用规则”，要求实质性交易协作前读取 `AGENTS.md`、`AGENT_STATE.md`、`memory.md`，并在具体股票、主题、日期、历史习惯问题上回查 `对话备份/` 与 `复盘/`。
- 更新 `memory.md`：新增“记忆系统运行原则”，记录用户希望助手像老熟人一样继承长期习惯，并明确后续必须把长期有效信息蒸馏进 `memory.md`。
- 更新 `AGENT_STATE.md`：补充当前记忆架构、工作流规则和后续需要整合 2026-04-26 之后归档的开放任务。

## 长期规则
后续不能把“对话已备份”视为“记忆已完成”。凡是出现长期有效的用户偏好、交易规则升级、反复执行错误、研究主线变化，都应同步更新 `memory.md`；具体细节则通过 `对话备份/` 与 `复盘/` 检索恢复。

## 2026-06-09 19:57 补充：最新持仓要求

用户进一步要求：每次聊天还要十分清楚其最新持仓情况。

已执行：
- 更新 `AGENTS.md`：新增“持仓状态优先规则”，要求交易相关对话前确认 `AGENT_STATE.md` 的最新持仓快照；持仓来源优先级为本轮券商截图/明确文字、`AGENT_STATE.md`、`交易记录.xlsx` 临时复核、历史记忆。
- 更新 `memory.md`：新增“持仓感知要求”，记录用户要求助手每次交易相关对话必须先考虑当前持仓影响。
- 更新 `AGENT_STATE.md`：新增 `Current Holdings Snapshot`。

用户随后明确更正：`思源电气` 已经清仓。

已执行：
- 将 `AGENT_STATE.md` 当前持仓快照修正为不再包含 `思源电气`。
- 标注 `交易记录.xlsx` 中关于 `思源电气` 的历史 `剩余仓位` 信号为 stale/dirty，不得作为当前持仓。
- 当前记录持仓只保留：`亨通光电` 1800 股、`生益科技` 500 股；仍需以最新券商持仓截图或用户明确文字作为更高优先级来源。
