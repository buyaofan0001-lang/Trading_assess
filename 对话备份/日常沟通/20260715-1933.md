# 2026-07-15 19:33 对话备份

## 用户请求

用户要求将当前的投资者/投资研究 skills 用一个技能全部串联，使它们能够相互协作。

## 架构决定

- 不新增重复入口，直接将已有 `$investor-council` 升级为“投资研究总控委员会”。
- 所有模块共享一份 canonical evidence packet，避免不同技能使用不同日期、口径或持仓事实。
- 默认在同一任务中顺序运行各分析镜头，不依赖子代理。

## 协作链路

1. `serenity-skill`：产业链映射、稀缺层、候选池、证据强度。
2. `buffett-investment-research`：能力圈、护城河、管理层、所有者经济、资产负债表和估值。
3. `buffett-perspective`：只补所有者思维、耐心、能力圈和市场情绪挑战，不重复写公司研究。
4. `munger-perspective`：反向思考、激励结构、偏误叠加、最强反证。
5. 内置马克斯席位：周期、共识、价格依赖风险和情景分布。
6. 内置林奇席位：公司分类和故事到数字验证。
7. 内置李录席位：长期可预测性、竞争结构、诚信、制度风险和安全边际。

## 实现内容

- 重写 `/Users/liuguan1/.codex/skills/investor-council/SKILL.md`，增加主题扫描、单公司、候选对比、当前持仓和事后复盘五类路由。
- 新增 `references/orchestration.md`，定义依赖注册表、模块交接 schema、证据共享、冲突处理与人格边界。
- 新增只读脚本 `scripts/check_dependencies.py`，检查 Serenity、Buffett Research、Buffett Perspective、Munger Perspective 是否存在。
- 重新生成 `agents/openai.yaml`，显示名称为“投资研究总控委员会”。

## 风控与裁决

- 不使用平均分或多数投票。
- 否决顺序：证据/会计/治理 → 偿债/流动性 → 能力圈 → 产业链价值捕获 → 公司质地 → 价格 → 当前执行。
- 研究结论和执行结论分开；即使研究通过，也可因仓位集中、技术失效、公告窗口或恢复期规则输出 `Execution blocked`。
- 默认不模仿巴菲特/芒格第一人称，除非用户显式要求角色扮演。

## 验证

- 依赖脚本在当前环境识别到四个上游技能全部存在。
- 空技能目录测试能正确返回缺失状态。
- Python 编译通过。
- Codex `quick_validate` 通过。

## 调用

重启 Codex 后只需调用 `$investor-council`。示例：“用 `$investor-council` 完整会审长电科技，先核对我的持仓，再分开研究结论和执行结论。”
