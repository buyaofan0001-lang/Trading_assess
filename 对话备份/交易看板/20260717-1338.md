# 交易看板对话备份｜2026-07-17 13:38

## 用户反馈

用户在实际打开的同行擂台页面中没有看到ATR和波动率指标。

## 诊断

- 真实 `/api/dashboard` 已经返回完整波动指标。
- 用户当前页面页脚仍为V1.8，DOM中没有 `.volatility-boundary`，加载的是无版本号的旧 `app.js` 和 `styles.css`。
- 根因是浏览器静态资源缓存，不是ATR计算或行情接口失败。

## 修复

- HTML将CSS/JS升级为带 `?v=1.9.1` 的版本化资源。
- 服务端对HTML、CSS和JS发送 `no-store, no-cache, must-revalidate, max-age=0`，避免以后前端升级继续命中旧脚本。
- 页脚升级为V1.9并标注“持仓波动边界”。
- 已把用户现有标签页切换到版本化地址并保留。

## 验证

- 浏览器实际DOM显示两块波动边界：科创50ETF与长电科技各一块。
- 每块均显示ATR(14)、当前跌幅ATR倍数、盘中最大下探及20日年化波动率。
- 静态响应头校验通过；Python语法检查通过；Playwright 6项测试全部通过。
