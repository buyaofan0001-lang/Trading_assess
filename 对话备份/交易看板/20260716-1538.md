# 2026-07-16 15:38 · 删除看板生活恢复模块

## 用户需求

用户明确要求删除交易看板中的“生活恢复”这一项。

## 本轮处理

- 删除左侧“生活恢复”导航和完整页面区块，日记调整为最后一项，手机导航恢复为5列。
- 删除前端恢复清单渲染、勾选状态与 localStorage 读写逻辑。
- 删除 `/api/dashboard` 中的 `recovery` 负载及 `config.json` 的 `recovery_checklist` 配置。
- 删除恢复模块专属样式和自动化测试中的清单勾选断言。
- 看板版本更新为V1.5，并同步更新README与项目状态。
- 保留 `交易生活执行手册.md`、恢复期行为闸门和首页交易权限限制；本次只删除看板展示模块，不代表取消风控规则。

