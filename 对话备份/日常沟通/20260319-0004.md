## 对话记录

> [!info] 记录时间
> `2026-03-19 00:04`

> [!abstract] 交易标的
> 日常沟通

> [!question] 用户
> /Users/liuguan1/Documents/github/Trading_assess/量化数据/stock_LM/stock_lgbm_model.py 你认为这个模型还有什么需要改进的地方 阅读脚本 可以从特征的选择 以及 使用lm的方法来提意见

> [!note] 助手
> | 严重度 | 发现 | 位置 |
> | --- | --- | --- |
> | 高 | 内层 early stopping 验证集有时间泄漏。外层 `EMBARGO` 生效了，但 `train_lgbm()` 里把训练窗最后 10% 直接拿去做验证，没有再 purge 20 天；对未来 20 日标签来说，这会污染 `best_iteration`。这是我认为最先要修的。 | [stock_lgbm_model.py](/Users/liuguan1/Documents/github/Trading_assess/量化数据/stock_LM/stock_lgbm_model.py#L122), [stock_lgbm_model.py](/Users/liuguan1/Documents/github/Trading_assess/量化数据/stock_LM/stock_lgbm_model.py#L261) |
> | 高 | 目标和优化目标不对齐。你训练的是 `target_rank_20d`，最终关心的是 `IC / Top-N / 超额收益`，但 LightGBM 用的是 `regression + rmse`。这会让模型朝“数值拟合”而不是“截面排序”收敛。 | [stock_lgbm_model.py](/Users/liuguan1/Documents/github/Trading_assess/量化数据/stock_LM/stock_lgbm_model.py#L66), [stock_lgbm_model.py](/Users/liuguan1/Documents/github/Trading_assess/量化数据/stock_LM/stock_lgbm_model.py#L70), [stock_lgbm_model.py](/Users/liuguan1/Documents/github/Trading_assess/量化数据/stock_LM/stock_lgbm_model.py#L155) |
> | 高 | 特征冗余偏重。大量 `raw + rank` 双份并存，比如 `ret_20d/rank_ret_20d`、`ma20_dev/rank_ma20_dev`、`pe_zscore/rank_pe_zscore`，再叠加一整簇高相关的活跃度特征，容易让树模型重要性失真、让 Ridge 吃共线性。 | [stock_lgbm_model.py](/Users/liuguan1/Documents/github/Trading_assess/量化数据/stock_LM/stock_lgbm_model.py#L33), [stock_lgbm_model.py](/Users/liuguan1/Documents/github/Trading_assess/量化数据/stock_LM/stock_lgbm_model.py#L58) |
> | 高 | 特征库缺少真正正交的横截面因子。现在大多是价格、估值、换手、市值，比较容易学成风格暴露；缺行业中性动量、残差动量、beta、行业内相对估值、质量/预期修正这类更稳的 20 日因子。 | [stock_lgbm_model.py](/Users/liuguan1/Documents/github/Trading_assess/量化数据/stock_LM/stock_lgbm_model.py#L33), [stock_factor_builder.py](/Users/liuguan1/Documents/github/Trading_assess/量化数据/stock_LM/stock_factor_builder.py#L93) |
> | 中高 | 当前不少特征更偏短线博弈，不太匹配 20 日持有。`lower_shadow`、`upper_shadow`、`body_pct`、`is_limit_up`、`dist_limit_up`、`limit_up_cnt_*` 更像日内/短线事件特征，建议改成“事件后持续性”特征。 | [stock_lgbm_model.py](/Users/liuguan1/Documents/github/Trading_assess/量化数据/stock_LM/stock_lgbm_model.py#L47), [stock_lgbm_model.py](/Users/liuguan1/Documents/github/Trading_assess/量化数据/stock_LM/stock_lgbm_model.py#L54) |
> | 中高 | `fillna(0)` 不适合直接给 LightGBM。树模型本来能原生处理缺失，统一补 0 会把“真实 0”和“缺失”混在一起，尤其对 zscore、比率、估值类因子影响更大。 | [stock_lgbm_model.py](/Users/liuguan1/Documents/github/Trading_assess/量化数据/stock_LM/stock_lgbm_model.py#L272), [stock_lgbm_model.py](/Users/liuguan1/Documents/github/Trading_assess/量化数据/stock_LM/stock_lgbm_model.py#L277) |
> | 中 | `Ridge` 基线偏弱，不能很好证明 LGBM 优势。`alpha=1.0` 没调，输入高度共线，而且目标是截面 rank，却直接拿池化样本做线性回归。这个 baseline 更像占位符。 | [stock_lgbm_model.py](/Users/liuguan1/Documents/github/Trading_assess/量化数据/stock_LM/stock_lgbm_model.py#L142) |
> | 中 | 评估与策略定义没有严格绑定。`evaluate_rebal()` 用 `dates[::TEST_DAYS]` 近似调仓日，不是直接拿每个 fold 的 `test_dates[0]`，如果中间有 fold 被跳过或某天被过滤掉，回测日期会漂。 | [stock_lgbm_model.py](/Users/liuguan1/Documents/github/Trading_assess/量化数据/stock_LM/stock_lgbm_model.py#L164), [stock_lgbm_model.py](/Users/liuguan1/Documents/github/Trading_assess/量化数据/stock_LM/stock_lgbm_model.py#L254) |
> | 中 | SHAP 现在更像展示，不像研究证据。你只在每 10 折、且用训练集样本做 SHAP，没有 fold-level 稳定性，也没有 ablation，所以还不能据此决定保留哪些特征。 | [stock_lgbm_model.py](/Users/liuguan1/Documents/github/Trading_assess/量化数据/stock_LM/stock_lgbm_model.py#L227), [stock_lgbm_model.py](/Users/liuguan1/Documents/github/Trading_assess/量化数据/stock_LM/stock_lgbm_model.py#L297) |
>
> 我会把改进优先级定成这四步。
>
> 1. 先修研究框架：内层验证也做 purge/embargo，并把调仓日直接绑定到 fold 起点。
> 2. 再修目标设计：如果你想做 Top-N 选股，优先考虑按 `trade_date` 分组的排序学习，至少也要把 early stopping 指标改成 RankIC 一类的排序指标；如果继续做回归，我更倾向直接预测行业/风格中性后的 `future residual return`，而不是预测百分位本身。
> 3. 重构特征库：先删重复编码，只保留一套主表达。我会优先保留截面 rank 或行业中性后的标准化版本，而不是 raw 和 rank 同时上。然后补三类因子：`行业/风格中性因子`、`质量/预期修正因子`、`事件后持续性因子`。
> 4. 重做 baseline：用更干净的线性基线，比如按日做截面标准化后的 `Ridge/ElasticNet`，再和 LightGBM 比；否则现在这个 ensemble 参考价值有限。
>
> 如果只允许我给一个“最值得先改”的点，我会选：先把内层 early stopping 泄漏修掉，再重新看 IC 和 Top-N 超额；在这之前，当前回测数字我不会太信。
>
> 这次我只做了静态审阅，没有跑实验。补充说明一下：本地环境里 `pandas / numpy / parquet` 有兼容问题，我尝试直接读 parquet 时失败了，所以这里的结论基于脚本结构本身，不是基于复跑结果。
