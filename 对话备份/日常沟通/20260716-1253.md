# rt_min 权限开通请求

- 时间：2026-07-16 12:53
- 用户请求：开通 `rt_min`。

## 当前状态

- 当前自建 Tushare 地址调用 `rt_min` 与 `rt_min_daily` 均返回“请联系管理员添加此权限”。
- 这是服务端权限，无法通过修改本地 Python 代码开通。
- Tushare 官方权限说明将 A 股实时分钟列为独立权限；但当前项目使用自建转发服务，官方购买是否能同步到该服务取决于服务商的 Token 绑定机制。

## 建议向接口管理员申请

请为当前 Token 开通：

1. `rt_min`：A股实时分钟，支持 1/5/15/30/60MIN。
2. `rt_min_daily`：当日分钟序列补齐。
3. `rt_k` 批量权限：看板批量刷新持仓及观察列表。
4. 明确单次股票数量、每分钟调用次数、行情延迟和服务时间。

## 开通后验收

- 单股票 `rt_min` 返回最新分钟 OHLC、成交量、成交额和行情时间。
- 多股票 `rt_min` 能按管理员声明的批量数返回。
- `rt_min_daily` 能返回当日从开盘至当前的完整分钟序列。
- 连续轮询不触发限流，并验证午间、收盘与断线状态。

本轮未发生购买，也未获得或修改服务端权限。
